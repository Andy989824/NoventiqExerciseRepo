# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main


pool: 'my-laptop-agent'

variables:
- group: my-variable-group-01

steps:
- script: |
          echo $(variable01) 
          echo $(dbURL)
  displayName: 'Checking Variable Group'

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Visual Studio Enterprise Subscription – MPN(d957954d-fa30-4f4d-aed1-d1aea92276c7)'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      # Retrieve the variable values from the Azure DevOps Variable Group
      DB_CONNECTION_URL=$(dbURL)

      kubectl apply -f aksManifestFile.yaml


      





