# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main


pool: my-laptop-agent

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- script: echo $(variable01)

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Visual Studio Enterprise Subscription – MPN(1)(d957954d-fa30-4f4d-aed1-d1aea92276c7)'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript:
      #Retrieve the variable values from the Azure DevOps Variable Group
      DB_CONNECTION_URL=$(DB_CONNECTION_URL)

