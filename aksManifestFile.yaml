apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-deployment-frontend-4
  labels:
    app: my-app-deployment-frontend-4
spec:
  replicas: #replicaNo#
  selector:
    matchLabels:
      app: my-app-deployment-frontend-4
  template:
    metadata:
      labels:
        app: my-app-deployment-frontend-4
    spec:
      containers:
        - name: my-app-deployment-frontend-4
          image: myappacr1998001.azurecr.io/sampleapp:appTag-95
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: my-app-deployment-frontend-4
spec:
  selector:
    app: my-app-deployment-frontend-4
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-deployment-backend-4
  labels:
    app: my-app-deployment-backend-4
spec:
  replicas: #replicaNo#
  selector:
    matchLabels:
      app: my-app-deployment-backend-4
  template:
    metadata:
      labels:
        app: my-app-deployment-backend-4
    spec:
      containers:
        - name: my-app-deployment-backend-4
          image: myappacr1998001.azurecr.io/sampleapp:serverTag-95
          ports:
            - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: my-app-deployment-backend-4
spec:
  selector:
    app: my-app-deployment-backend-4
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: LoadBalancer
